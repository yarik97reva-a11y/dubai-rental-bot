# Быстрый старт 🚀

## За 5 минут до первого запуска

### 1. Получите токены (2 минуты)

**Telegram Bot Token:**
- Откройте [@BotFather](https://t.me/BotFather)
- Отправьте: `/newbot`
- Следуйте инструкциям
- Скопируйте токен

**Ваш Chat ID:**
- Откройте [@userinfobot](https://t.me/userinfobot)
- Отправьте любое сообщение
- Скопируйте ваш ID

### 2. Локальный запуск (3 минуты)

```bash
# Клонируйте и перейдите в папку
cd dubai-rental-bot

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env и вставьте ваши токены
# TELEGRAM_BOT_TOKEN=ваш_токен
# TELEGRAM_CHAT_ID=ваш_chat_id

# Запустите бота
cd src && python bot.py
```

### 3. Проверьте работу

1. Найдите вашего бота в Telegram по имени, которое вы указали в BotFather
2. Отправьте `/start`
3. Отправьте `/scan` для тестового сканирования
4. Подождите несколько минут
5. Получите уведомления о новых объявлениях!

## Деплой на Render (бесплатно, 24/7)

### Быстрый деплой через Blueprint

1. Создайте репозиторий на GitHub
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

3. Зайдите на [render.com](https://render.com)
4. Нажмите **"New +" → "Blueprint"**
5. Выберите ваш репозиторий
6. Render автоматически создаст:
   - Worker service для бота
   - PostgreSQL базу данных
7. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
8. Нажмите **"Deploy"**

Готово! Бот работает 24/7 бесплатно.

## Основные команды

- `/start` - Начать работу
- `/scan` - Запустить сканирование вручную (не ждать ежедневного)
- `/stats` - Посмотреть статистику
- `/sites` - Какие сайты отслеживаются
- `/help` - Помощь

## Что дальше?

1. **Настройте фильтры** - отредактируйте `config/sites_config.json`
2. **Добавьте сайты** - читайте `ADDING_SITES.md`
3. **Измените время уведомлений** - в `.env` параметр `NOTIFICATION_TIME`

## Проблемы?

**Бот не отвечает:**
```bash
# Проверьте, что токен правильный
echo $TELEGRAM_BOT_TOKEN

# Проверьте логи
tail -f logs.txt
```

**Нет уведомлений:**
- Убедитесь, что `TELEGRAM_CHAT_ID` правильный
- Запустите `/scan` вручную для проверки

**Ошибки при парсинге:**
- Сайты могут измениться
- Обновите селекторы в `config/sites_config.json`

Подробности в [README.md](README.md)

---

**Всё получилось?** Отлично! Теперь вы будете получать уведомления о новых квартирах в Дубае каждый день! 🏠✨
