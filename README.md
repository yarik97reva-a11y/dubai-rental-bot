# Dubai Rental Bot 🏠

> 🎯 **БЫСТРЫЙ СТАРТ:** Откройте файл **[START_HERE.md](START_HERE.md)** для установки за 3 минуты!

Telegram бот для автоматического мониторинга новых объявлений об аренде недвижимости в Дубае.

---

## 🚀 Установка за 3 клика

Если вы хотите просто запустить бота - следуйте этой инструкции:

### 1️⃣ Откройте терминал в папке проекта

### 2️⃣ Запустите автоматическую установку:
```bash
python3 setup.py
```

### 3️⃣ Следуйте инструкциям на экране

**Готово!** Бот сам установит всё необходимое и попросит ввести только токены.

📖 **Подробная инструкция:** [START_HERE.md](START_HERE.md)

---

## Возможности

- ✅ Автоматический мониторинг нескольких сайтов с недвижимостью
- ✅ Ежедневные уведомления о новых объявлениях
- ✅ База данных для отслеживания уже просмотренных объявлений
- ✅ Легкое добавление новых сайтов через конфигурационный файл
- ✅ Деплой на Render с PostgreSQL
- ✅ Команды управления через Telegram

## Мониторимые сайты

По умолчанию бот отслеживает:
- Property Finder (propertyfinder.ae)
- Bayut (bayut.com)
- Dubizzle (dubizzle.com)

## Быстрый старт

### 1. Создание Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 2. Получение Chat ID

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Бот вернет ваш Chat ID
4. Сохраните Chat ID

### 3. Локальная установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd dubai-rental-bot

# Установите зависимости
pip install -r requirements.txt

# Скопируйте .env.example в .env
cp .env.example .env

# Отредактируйте .env и добавьте ваши данные
nano .env
```

### 4. Настройка переменных окружения

Отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_CHAT_ID=your_chat_id_from_userinfobot
NOTIFICATION_TIME=09:00
DATABASE_URL=sqlite:///properties.db
MAX_LISTINGS_PER_NOTIFICATION=50
```

### 5. Запуск бота локально

```bash
cd src
python bot.py
```

## Деплой на Render

### Автоматический деплой

1. Создайте репозиторий на GitHub и запушьте код
2. Зарегистрируйтесь на [Render](https://render.com)
3. Нажмите "New +" → "Blueprint"
4. Подключите ваш GitHub репозиторий
5. Render автоматически обнаружит `render.yaml` и настроит сервисы

### Настройка переменных окружения на Render

После создания сервиса:

1. Перейдите в Dashboard → Ваш сервис → Environment
2. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN` - ваш токен от BotFather
   - `TELEGRAM_CHAT_ID` - ваш Chat ID

3. `DATABASE_URL` будет автоматически настроен при создании PostgreSQL базы

### Ручной деплой

Если автоматический деплой не работает:

1. Создайте новый Web Service на Render
2. Подключите GitHub репозиторий
3. Настройки:
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `cd src && python bot.py`
4. Добавьте переменные окружения
5. Создайте PostgreSQL базу данных и подключите её

## Команды бота

После запуска бота, доступны следующие команды:

- `/start` - Приветственное сообщение
- `/scan` - Запустить сканирование вручную
- `/stats` - Показать статистику базы данных
- `/sites` - Список отслеживаемых сайтов
- `/settings` - Настройки бота
- `/help` - Помощь

## Добавление новых сайтов

Вы можете легко добавить новые сайты, отредактировав файл `config/sites_config.json`:

```json
{
  "name": "Название сайта",
  "enabled": true,
  "base_url": "https://example.com",
  "search_url": "https://example.com/rentals",
  "scraper_type": "custom",
  "selectors": {
    "listing_container": "div.property-card",
    "title": "h2.title",
    "price": "span.price",
    "location": "div.location",
    "link": "a.property-link",
    "bedrooms": "span.bedrooms",
    "area": "span.area"
  }
}
```

### Как найти правильные селекторы

1. Откройте сайт с недвижимостью в браузере
2. Нажмите F12 для открытия Developer Tools
3. Используйте инструмент выбора элемента (стрелка в углу)
4. Кликните на элемент объявления
5. В HTML коде найдите класс или ID контейнера
6. Повторите для каждого поля (цена, заголовок, локация и т.д.)

## Архитектура проекта

```
dubai-rental-bot/
├── config/
│   └── sites_config.json    # Конфигурация сайтов для парсинга
├── src/
│   ├── bot.py              # Основной файл Telegram бота
│   ├── scraper.py          # Веб-скрейпер для сайтов
│   └── database.py         # Управление базой данных
├── requirements.txt        # Python зависимости
├── .env.example           # Пример переменных окружения
├── .gitignore
├── Procfile               # Конфигурация для Render
├── render.yaml            # Автоматическая настройка Render
└── README.md
```

## Технологии

- **Python 3.9+**
- **python-telegram-bot** - Telegram Bot API
- **BeautifulSoup4** - Парсинг HTML
- **SQLAlchemy** - ORM для базы данных
- **APScheduler** - Планировщик задач
- **Requests** - HTTP запросы

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что токен бота правильный
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Не приходят уведомления

1. Проверьте `TELEGRAM_CHAT_ID` в переменных окружения
2. Убедитесь, что время уведомлений правильное (`NOTIFICATION_TIME`)
3. Запустите `/scan` вручную для проверки

### Ошибки парсинга

1. Сайты могут менять свою структуру
2. Обновите селекторы в `config/sites_config.json`
3. Проверьте логи для деталей ошибки

### База данных

- Локально используется SQLite (файл `properties.db`)
- На Render используется PostgreSQL (автоматически)
- Для миграции данных используйте SQLAlchemy

## Безопасность

- ⚠️ Никогда не коммитьте файл `.env` с токенами
- ⚠️ Не делитесь своим Bot Token
- ⚠️ Chat ID является приватной информацией

## Лицензия

MIT

## Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте этот README
2. Посмотрите логи приложения
3. Создайте Issue в GitHub репозитории

## Дальнейшее развитие

Идеи для улучшения:
- [ ] Фильтры по цене, району, количеству комнат
- [ ] Поддержка нескольких пользователей
- [ ] Веб-интерфейс для управления
- [ ] Уведомления о снижении цен
- [ ] Интеграция с большим количеством сайтов
- [ ] Поддержка других городов ОАЭ (Абу-Даби, Шарджа и т.д.)
- [ ] ML для рекомендаций на основе предпочтений

---

Создано с ❤️ для поиска идеального жилья в Дубае
