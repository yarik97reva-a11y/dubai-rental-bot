# Как добавить свой сайт для мониторинга

## Пошаговая инструкция

### Шаг 1: Определите структуру сайта

1. Откройте сайт с недвижимостью в браузере
2. Перейдите на страницу с поиском/списком недвижимости
3. Нажмите `F12` или `Ctrl+Shift+I` для открытия Developer Tools
4. Выберите инструмент "Select Element" (стрелка в углу Dev Tools)

### Шаг 2: Найдите CSS селекторы

Вам нужно найти селекторы для следующих элементов:

1. **Контейнер объявления** (`listing_container`)
   - Кликните на карточку объявления
   - Найдите общий контейнер, который обёртывает всё объявление
   - Пример: `div.property-card`, `article.listing`

2. **Заголовок** (`title`)
   - Кликните на название объявления
   - Найдите селектор для заголовка
   - Пример: `h2.property-title`, `a.listing-title`

3. **Цена** (`price`)
   - Кликните на цену
   - Найдите селектор
   - Пример: `span.price`, `div.property-price`

4. **Локация** (`location`)
   - Кликните на адрес/район
   - Пример: `div.location`, `span.address`

5. **Ссылка** (`link`)
   - Найдите тег `<a>` который ведёт на страницу объявления
   - Пример: `a.property-link`, `a[href*='/property/']`

6. **Количество спален** (`bedrooms`) - опционально
   - Пример: `span.bedrooms`, `div.specs span:first-child`

7. **Площадь** (`area`) - опционально
   - Пример: `span.area`, `div.sqft`

### Шаг 3: Создайте конфигурацию

Откройте файл `config/sites_config.json` и добавьте новый объект в массив `sites`:

```json
{
  "name": "Название сайта",
  "enabled": true,
  "base_url": "https://example.com",
  "search_url": "https://example.com/dubai/rent",
  "scraper_type": "custom",
  "selectors": {
    "listing_container": "div.property-card",
    "title": "h2.title",
    "price": "span.price",
    "location": "div.location",
    "link": "a.property-link",
    "bedrooms": "span.beds",
    "area": "span.sqft"
  }
}
```

### Шаг 4: Описание полей

| Поле | Обязательное | Описание |
|------|-------------|----------|
| `name` | ✅ | Название сайта для отображения |
| `enabled` | ✅ | `true` или `false` - включен ли парсинг |
| `base_url` | ✅ | Основной URL сайта |
| `search_url` | ✅ | URL страницы с результатами поиска |
| `scraper_type` | ✅ | Тип парсера (используйте "custom") |
| `selectors` | ✅ | Объект с CSS селекторами |

### Селекторы

| Селектор | Обязательный | Описание |
|----------|-------------|----------|
| `listing_container` | ✅ | Контейнер одного объявления |
| `title` | ✅ | Название объявления |
| `price` | ✅ | Цена |
| `location` | ✅ | Адрес/район |
| `link` | ✅ | Ссылка на объявление |
| `bedrooms` | ❌ | Количество спален |
| `area` | ❌ | Площадь |

## Примеры реальных конфигураций

### Пример 1: Простой сайт

```json
{
  "name": "Example Realty",
  "enabled": true,
  "base_url": "https://example-realty.com",
  "search_url": "https://example-realty.com/rent/dubai",
  "scraper_type": "custom",
  "selectors": {
    "listing_container": "div.listing",
    "title": "h3",
    "price": "span.price-tag",
    "location": "p.address",
    "link": "a",
    "bedrooms": "span.bedroom-count",
    "area": "span.size"
  }
}
```

### Пример 2: Сложные селекторы

```json
{
  "name": "Advanced Realty",
  "enabled": true,
  "base_url": "https://advanced-realty.com",
  "search_url": "https://advanced-realty.com/search?city=dubai&type=rent",
  "scraper_type": "custom",
  "selectors": {
    "listing_container": "article[data-type='property']",
    "title": "div.content h2.heading",
    "price": "div.price-container span.amount",
    "location": "div.details span[data-field='location']",
    "link": "a.property-link",
    "bedrooms": "ul.features li:contains('Bed') span",
    "area": "ul.features li:contains('sqft') span"
  }
}
```

## Тестирование конфигурации

После добавления конфигурации:

1. Сохраните файл `config/sites_config.json`
2. Перезапустите бота
3. Отправьте команду `/scan` в Telegram
4. Проверьте логи на наличие ошибок
5. Если данные парсятся неправильно, скорректируйте селекторы

## Частые проблемы

### 1. Не находит контейнеры объявлений

**Проблема**: `Found 0 listings on <Site Name>`

**Решение**:
- Проверьте селектор `listing_container`
- Попробуйте более общий селектор
- Убедитесь, что сайт не использует JavaScript для загрузки контента

### 2. Пустые поля

**Проблема**: Все поля `N/A`

**Решение**:
- Проверьте каждый селектор отдельно
- Используйте более специфичные селекторы
- Убедитесь, что селекторы относительны к `listing_container`

### 3. Неправильные ссылки

**Проблема**: Ссылки не работают

**Решение**:
- Убедитесь, что `base_url` правильный
- Проверьте, что селектор `link` указывает на тег `<a>`
- Бот автоматически преобразует относительные ссылки в абсолютные

### 4. Сайт блокирует запросы

**Проблема**: Ошибки 403 или 429

**Решение**:
- Увеличьте `delay_between_requests` в `scraping_settings`
- Проверьте `user_agent` в конфигурации
- Некоторые сайты требуют авторизации или используют Cloudflare

## Расширенные настройки

### Настройка парсинга

В разделе `scraping_settings` файла `sites_config.json`:

```json
"scraping_settings": {
  "delay_between_requests": 2,
  "max_pages_per_site": 3,
  "user_agent": "Mozilla/5.0..."
}
```

- `delay_between_requests` - задержка между запросами в секундах
- `max_pages_per_site` - максимальное количество страниц для парсинга
- `user_agent` - User-Agent для запросов

## Управление сайтами через Telegram

Вы также можете управлять сайтами через команды бота (в будущих версиях):

```
/sites - Показать все сайты
/enable <название> - Включить сайт
/disable <название> - Отключить сайт
```

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что селекторы правильные
3. Попробуйте упростить селекторы
4. Создайте Issue в GitHub репозитории с примером конфигурации

---

Удачи в добавлении новых сайтов! 🚀
